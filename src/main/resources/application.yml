spring:
  datasource:
    url: jdbc:mysql://localhost:3306/KTX-reservation1
    username: root
    password: itc801
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    database: mysql
    show-sql: true
    properties:
      hibernate:
        format_sql: true

server:
  port : 8090
